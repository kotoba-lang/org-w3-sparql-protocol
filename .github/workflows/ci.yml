name: CI
on:
  push:
    branches: [main]
  pull_request:

jobs:
  nbb-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: kotoba-lang/datom-source
          # Same pin as deps.edn — the IPatternSource contract source->quads
          # scans. Zero deps of its own.
          ref: b42f6e6ff942c7f8a2856f8142ebffc13aa11098
          path: .deps/datom-source
      - uses: actions/checkout@v4
        with:
          repository: kotoba-lang/sparql
          # Same pin as deps.edn -- keep the two in lockstep.
          ref: 1029b50cbbbdbc4f76a7c2e7bf86728068ef368b
          path: .deps/sparql
      - uses: actions/checkout@v4
        with:
          repository: kotoba-lang/kotobase-query
          # Same pin as deps.edn -- keep the two in lockstep.
          ref: b8bb880d56d242f9a98bd4a9c0375abb4b633bdb
          path: .deps/kotobase-query
      - uses: actions/checkout@v4
        with:
          repository: kotoba-lang/kotobase
          # Same pin as kotobase-query's own deps.edn (transitive).
          ref: bbf57df9dde5c1869182012860129a598382e088
          path: .deps/kotobase
      - uses: actions/checkout@v4
        with:
          repository: kotoba-lang/arrangement
          # Same pin as kotobase-query's own deps.edn (transitive).
          ref: 9e90426430bb4eb6206112a74465a4f9d62fe0c8
          path: .deps/arrangement
      - uses: actions/checkout@v4
        with:
          repository: kotoba-lang/prolly-tree
          # Same pin as arrangement's own deps.edn (transitive).
          ref: 2a5ba48a2eb11cc73b549ee75f5c1b71cc223566
          path: .deps/prolly-tree
      - uses: actions/checkout@v4
        with:
          repository: kotoba-lang/io-ipld
          # Same pin as arrangement's own deps.edn (transitive).
          ref: e08dc3b2ab705be292e388cd1ad136381544a041
          path: .deps/io-ipld
      - uses: actions/checkout@v4
        with:
          repository: kotoba-lang/io-multiformats
          # Same pin as io-ipld's own deps.edn (transitive).
          ref: eec3ee85c458ea830be94518b9a42f4a8f1aee40
          path: .deps/io-multiformats
      - uses: actions/checkout@v4
        with:
          repository: kotoba-lang/org-ietf-cbor
          # Same pin as io-ipld's own deps.edn (transitive).
          ref: de18ad29102f9df150e214d756de4719c3c00d7a
          path: .deps/org-ietf-cbor
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --global nbb
      - run: npm install
      - run: nbb --classpath "src:test:.deps/datom-source/src:.deps/sparql/src:.deps/kotobase-query/src:.deps/kotobase/src:.deps/arrangement/src:.deps/prolly-tree/src:.deps/io-ipld/src:.deps/io-multiformats/src:.deps/org-ietf-cbor/src" bin/run_tests.cljs

  jvm-compat:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - uses: DeLaGuardo/setup-clojure@13.4
        with:
          cli: latest
      - run: clojure -M:test
      - run: clojure -M:lint
